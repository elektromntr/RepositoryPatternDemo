<!DOCTYPE html>

<html>
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
    @*Bootstrap*@
    @Styles.Render("~/Content/")
</head>
<body>
    <nav id="myNavbar" class="navbar navbar-default navbar-inverse navbar-fixed-top" role="navigation">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbarCollapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            @Html.ActionLink("Bohaterowie", "Index", "Home", new { }, new { @class = "navbar-brand" })
        </div>
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="navbarCollapse">
            <ul class="nav navbar-nav">
                <li class="active">
                    @Html.ActionLink("Wrocław", "Index", "Home", new { city = "Wrocław" }, null)
                </li>
                <li class="active">
                    @Html.ActionLink("Warszawa", "Index", "Home", new { city = "Warszawa" }, null)
                </li>
                <li class="active">
                    @Html.ActionLink("Lublin", "Index", "Home", new { city = "Lublin" }, null)
                </li>
            </ul>
        </div>
    </nav>
    <div>
        @RenderBody()
    </div>
    @*Scripts*@
    @Scripts.Render("~/Scripts/")
</body>
</html>
